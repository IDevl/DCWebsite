function getProjects() {
    fetch('https://jeffersondc.netlify.app/api/project')
        .then(function (res) {
            return res.json();
        }).then(function (data) {
            // This is an array so we have to loop through it
            let output = '';
            data.forEach(function (character) {
                output += `
                    <div id="project" class="d-flex flex-row flex-wrap align-items-center">
                        <div class="col-lg d-flex align-items-center justify-content-center px-0">
                            <img class="w-100" src="${character.image}" />
                            <div id="projectLinkBackdrop">
                                <a href="{}" target="_blank" id="projectLink"> See Project </a>
                            </div>
                        </div>
                        <div class="col-lg px-0">
                            <h2 class="text-left"> {} </h2>
                            <p class="text-left mt-2"> {} </p>
                            <div class="col-lg d-flex flex-row flex-wrap justify-content-start align-items-start mt-3 p-0"
                                style="gap: 0.75em;">
                                <div class="skill d-flex align-items-center"> {} </div>
                            </div>
                        </div>
                    </div>
                `;
            });
            document.getElementById('projectsSliderSample').innerHTML = output;
            AOS.init({ duration: "1000", once: true });

        })
        .catch(function (err) {
            console.log(err);
        });
}

getProjects();